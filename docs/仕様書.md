# ココフォリア カットイン素材ジェネレータ 設計仕様書

任意の日本語テキストから、虹色グラデ＋袋文字＋集中線などの装飾アニメ画像を生成するWebアプリ。
**用途はオンセツール「ココフォリア」のカットイン素材**。
参考UI: MEGAMOJI (https://zk-phi.github.io/MEGAMOJI/)

---

## 0. スコープと差別化

| 項目 | 内容 |
|---|---|
| 入力 | テキスト（複数行可）、カラーテーマ、演出テンプレ、出力サイズ |
| 出力 | **APNG**（カットイン・ステッカー）／**GIF**（Discordチャット添付）／静止PNG |
| 非スコープ | 画像アップロード、音源、ユーザー登録、サーバ保存、**Discord絵文字** |

MEGAMOJI は「任意画像＋汎用エフェクト」だが、本アプリは
**「ココフォリアのカットイン用・日本語装飾文字に特化」** することで差別化する。
装飾は袋文字＋メタリック縁＋虹グラデを既定とするが、**文字装飾・カラー・演出の3軸を
独立したテンプレとして持ち、組み合わせられる**ことが中身の差別化になる（6.0）。

### 0.1 ココフォリア側の仕様（確認済み・2026-09時点）

| 項目 | 内容 | 出典 |
|---|---|---|
| 対応形式 | JPG / PNG / GIF / **APNG** | 公式docs |
| カットインの表示 | 画面中央に一時表示。盤面の上に重なる | 公式docs |
| 終了条件 | 音源を設定するとその再生終了で消える。**音源未設定なら操作するまで消えない** | 公式docs |
| 発火 | カットイン一覧から手動 / チャット末尾がタイトルと一致 | 公式docs |
| 容量 | 「各ファイルごとに容量制限」とのみ記載。具体値は非公開 | 公式FAQ |
| 容量（実務値） | 1MB制限との情報あり。配布物は1MB未満推奨 | コミュニティ（要検証） |
| 表示上限480px | v1.3.0時点の情報。現行バージョンでの真偽は**未確認** | 個人ツイート（要検証） |

### 0.3 配布先ごとの必須形式（最重要・ここを間違えると全部やり直し）

| 配布先 | 必須形式 | 透過 | 容量 | 寸法 |
|---|---|---|---|---|
| ココフォリア カットイン | APNG（GIFも可） | 8bit | 約1MB | 自由 |
| Discord ステッカー | **APNG**（GIF不可） | 8bit | 512KB | **320×320 厳密** |
| Discord チャット添付 | **GIF**（APNG不可） | 1bit | 実質無制限 | 自由 |

**Discord は用途によって必須形式が反転する。**
ステッカーはGIFを受け付けず、チャット添付はAPNGをアニメーションさせない
（サーバー側のリサイズ処理がAPNGのフレームチャンクを剥がし、1フレーム目の静止画になる。
Discordの画像処理ライブラリ lilliput の既知の挙動で、数年来報告され続けている）。

したがって **APNG と GIF の両方を第一級の出力として実装する**。
どちらかを「互換用の後回し」にはできない。

### 0.4 用途から導かれる設計上の決定

1. **APNGとGIFの両方を第一級の出力にする（0.3）。** カットインとステッカーはAPNG、
   Discordチャット添付はGIF。ココフォリアのカットインは盤面の上に重なるため透過が必須で、
   GIFの1bit透過ではアンチエイリアス縁が白フチのギザギザになる。APNGは8bitアルファで綺麗に抜ける。
2. **全アニメはシームレスループ型に限定する。** 音源なしカットインは無限ループ再生されるため、
   「登場→静止」のワンショット演出は2周目以降が破綻する。ワンショット系テンプレは作らない。
3. **サイズ予算は 1MB、目標 300〜500KB。** APNGは無損失だと容易に数MBになる。量子化必須。
4. **正方形固定をやめる。** カットインは横帯型（例 1200×300）も定番。キャンバスは `{w, h}` で持つ。
5. **TRPG文言プリセットとタイトル文字列の提案をUIに組み込む。** カットインはチャット末尾の文字列で
   発火するため、「成功」「失敗」「クリティカル」等の定型と相性が良い。
   MEGAMOJIには無い、この用途固有の価値になる。
6. **配布先はターゲット記述子として抽象化する（7.4）。** 第一目標はココフォリアだが、
   Discordステッカー（320×320固定・512KB・APNG）は同じパイプラインにそのまま乗る。
   Discord絵文字（128×128・256KB・GIFのみ・表示32px）は可読性の制約が別物なので、
   別デザインモードとして扱う（7.5）。

---

## 1. 技術選定

| レイヤ | 採用 | 理由 |
|---|---|---|
| ビルド | Vite + TypeScript | 設定が軽く、Worker のバンドルが標準対応 |
| UI | React 18 | 実装エージェントに投げる際の情報量が最大 |
| スタイル | Tailwind CSS | UI をコードだけで完結させる |
| 描画 | Canvas 2D（メインスレッド） | Worker 内の日本語Webフォントは環境依存のため避ける |
| **APNGエンコード** | **UPNG.js**（Web Worker 上） | `UPNG.encode(frames, w, h, cnum)` が量子化とAPNG生成を一括で行う。アルファを保ったまま減色できる唯一の実用的な選択肢 |
| GIFエンコード | gifenc（同Worker、互換用） | `gif.js` は旧く低速 |
| 乱数 | 自前の xorshift32（シード指定可） | 同じパラメータで同じ絵が出る＝URL共有が成立する条件 |
| 配信 | GitHub Pages | サーバ不要 |

**依存を増やさない方針**：色変換（HSL→RGB）、イージング、乱数は自前実装。数十行で済む。

---

## 2. アーキテクチャ

```
src/
  core/                 # UI非依存。ここだけでGIFが作れること
    types.ts            # 全パラメータ型
    color.ts            # ColorRamp 実装群
    random.ts           # シード付きPRNG
    easing.ts
    text/
      layout.ts         # 自動フォントサイズ・改行
      draw.ts           # 袋文字レンダリング
    layers/
      index.ts          # レイヤーレジストリ
      radiate.ts        # 集中線
      sparkle.ts
      confetti.ts
      bgFlash.ts
    render.ts           # params + t -> Canvas へ1フレーム描画
    frames.ts           # params -> ImageData[]
    templates.ts        # プリセット定義
    serialize.ts        # params <-> URLハッシュ
  worker/
    encode.worker.ts    # ImageData[] -> GIF Blob
  ui/
    App.tsx
    Preview.tsx         # rAFでリアルタイム再生
    Controls/*.tsx
    TemplateGallery.tsx
  fonts/fonts.css
```

**設計原則**：`core/` は DOM の `canvas` 以外に依存しない。UI を差し替えてもコアが動くこと。

### データフロー

```
RenderParams
   ↓ frames.ts:  for i in 0..N-1 →  t = i/N  →  render(ctx, params, t)
ImageData[]
   ↓ postMessage (transferable)
Worker: quantize → applyPalette → GIFEncoder
   ↓
Blob → ObjectURL → <img> / ダウンロード
```

---

## 3. 型定義（実装の起点）

```ts
// core/types.ts

export type Vec2 = { x: number; y: number };

/** 位置 u∈[0,1]（グラデ方向）と時刻 t∈[0,1) から色を返す */
export interface ColorRamp {
  kind: 'solid' | 'rainbow' | 'linear' | 'metal';
  /** @returns 'rgb(r,g,b)' or '#rrggbb' */
  sample(u: number, t: number): string;
  /** Canvas に流し込む用。矩形領域を渡すと CanvasGradient を返す */
  toGradient(ctx: CanvasRenderingContext2D, box: Box, t: number): string | CanvasGradient;
}

export type Box = { x: number; y: number; w: number; h: number };

/** シリアライズ可能な色指定（ColorRamp のファクトリ引数） */
export type ColorSpec =
  | { kind: 'solid'; color: string }
  | { kind: 'rainbow'; saturation: number; lightness: number; cycles: number; speed: number; angle: number }
  | { kind: 'linear'; stops: Array<{ at: number; color: string }>; angle: number; scrollSpeed: number }
  | { kind: 'metal'; base: 'gold' | 'silver'; angle: number };

/** 文字装飾の1パス。背面から順に適用される */
export type TextDecoSpec =
  /** 縁取り。widthRatio は fontSize 比 */
  | { type: 'stroke'; widthRatio: number; color: ColorSpec }
  /** 本体の塗り */
  | { type: 'fill'; color: ColorSpec }
  /** 3D押し出し。angle方向に depth 分だけ複製を重ねる */
  | { type: 'extrude'; depth: number; angle: number; color: ColorSpec }
  /** ドロップシャドウ。blur=0 でハードシャドウ */
  | { type: 'shadow'; blur: number; offset: Vec2; color: string; opacity: number }
  /** ネオン発光。同じ描画を blur を変えて重ねる */
  | { type: 'glow'; color: ColorSpec; radius: number; passes: number; intensity: number }
  /** 位置をずらした複製。blend='screen' でグリッチのRGBずれになる */
  | { type: 'offsetCopy'; offset: Vec2; color: string; blend: GlobalCompositeOperation; jitter: number }
  /** ストライプ・チェック・ドットなどのパターン塗り */
  | { type: 'pattern'; kind: 'stripe' | 'check' | 'dots'; colors: [string, string]; scale: number; angle: number; scrollSpeed: number }
  /** 文字の形で背景をくり抜く（destination-out） */
  | { type: 'knockout' };

export interface TextSpec {
  lines: string[];              // 明示改行。自動折返しはしない（意図しない改行が事故になる）
  fontId: string;               // FONTS のキー。fontFamily/weight は fonts.ts が持つ
  /** 文字サイズは自動算出。ユーザーは倍率のみ触る */
  scale: number;                // 0.5 - 1.2
  lineHeight: number;           // 1.0 - 1.4
  letterSpacing: number;        // em
  /** 背面→前面の順。装飾テンプレはこの配列を差し替えるだけ */
  decorations: TextDecoSpec[];
  /** 1文字ずつのアニメ（波打ち等）を有効にするか */
  perChar: boolean;
}

export interface LayerSpec {
  type: string;                 // 'radiate' | 'sparkle' | ...
  z: 'back' | 'front';
  params: Record<string, number | string | boolean>;
}

export type OutputFormat = 'apng' | 'gif' | 'png';

export interface RenderParams {
  version: 1;
  canvas: { w: number; h: number };   // 正方形とは限らない（横帯カットイン）
  frameCount: number;
  fps: number;
  seed: number;
  /** カットインは盤面に重なるため transparent が既定 */
  background: ColorSpec | { kind: 'transparent' };
  text: TextSpec;
  layers: LayerSpec[];
  /** 文字全体に掛かる動き */
  motion: { type: 'none' | 'pulse' | 'shake' | 'bounce' | 'rotate'; amount: number };
  output: {
    format: OutputFormat;
    /** APNG: 量子化色数。0=無損失。既定 256 */
    colors: number;
    /** バイト上限。超過時に警告 */
    budgetBytes: number;
    /** GIF出力時、半透明ピクセルをこの色で合成して不透明化する。null=透過のまま */
    matte: string | null;
  };
}

/** 全レイヤーの共通インタフェース */
export interface Layer {
  readonly type: string;
  /**
   * @param t 0<=t<1 の位相。t=0 と t=1 の見た目が一致すること（ループ保証）
   * @param rng seed から派生した決定論的乱数（フレーム間で同じ系列を再生成すること）
   */
  draw(ctx: CanvasRenderingContext2D, t: number, ctxInfo: DrawContext): void;
}

export interface DrawContext {
  w: number;
  h: number;
  /** 長さ系パラメータの基準。min(w, h) を使う */
  unit: number;
  center: Vec2;
  /** テキストの外接矩形。集中線の内半径決定などに使う */
  textBox: Box;
  rng: () => number;            // 呼ぶたびに進む。draw の先頭で必ず reseed する
  params: Record<string, any>;
}
```

### ループ保証のルール（重要）

- レイヤーは `t` の周期1の関数としてのみ実装する。`Math.sin(2πt·k)`、`(t*k)%1` のように書く。
- 乱数は「フレーム間で不変な属性」（線の角度など）にのみ使い、毎フレーム同じシードから引き直す。
  フレームごとに乱数を進めるとループが破綻する。
- レビュー時のチェック項目：フレーム0とフレームN-1が滑らかに繋がるか。

---

## 4. テキスト描画

### 4.1 自動フォントサイズ

```
1. 描画領域 = size * 0.92 の正方形（マージン確保）
2. fontSize を二分探索（8px 〜 size*1.2、10回程度）
   - ctx.font 設定 → 各行 measureText().width の最大値
   - 高さ = lines.length * fontSize * lineHeight
   - 幅・高さの両方が領域に収まる最大値を採用
3. 縁取り幅ぶん（strokes の widthRatio 合計）を外側に食うので領域から先に引いておく
```

文字数が変わっても崩れないことが本アプリの核なので、ここは最初に実装して単体テストを書く。

### 4.2 文字装飾パス（袋文字はこの特殊ケース）

`text.decorations` を背面から順に適用する。各パスの実装は `core/text/deco/` に1ファイルずつ置き、
`core/text/deco/index.ts` のレジストリに登録する。**追加は1ファイル＋1行**。

```ts
export interface TextDecorator {
  readonly type: string;
  draw(ctx: CanvasRenderingContext2D, glyphs: GlyphRenderer, t: number, c: TextDrawContext): void;
}

/** 文字形状の描画を抽象化。全デコレータはこれ経由で描く */
export interface GlyphRenderer {
  fill(ctx: CanvasRenderingContext2D, paint: Paint): void;
  stroke(ctx: CanvasRenderingContext2D, paint: Paint, lineWidth: number): void;
  /** 文字形状を白1色で描いたオフスクリーンを返す。マスク合成用。バッファは使い回す */
  mask(): CanvasImageSource;
  box: Box;
}

type Paint = string | CanvasGradient | CanvasPattern;
```

#### マスク合成方式（重要）

グラデ以外の塗り（パターン、複雑なグロー）は `fillStyle` だけでは表現しきれない。
汎用手段としてマスク合成を用意する。

```
1. オフスクリーンAに文字を白で描く（= マスク）
2. オフスクリーンBに塗り（虹グラデ／ストライプ／なんでも）を全面描画
3. Bに globalCompositeOperation='destination-in' でAを描く → 文字型に切り抜かれる
4. Bを本体キャンバスに合成
```

オフスクリーンは**2枚を使い回す**。パスごとに `createElement('canvas')` するとフレーム描画が破綻する。

#### 実装上の注意

- `lineJoin='miter'` は鋭角で棘が出る。`stroke` パスでは必ず `round`。
- `stroke` の `lineWidth` は中心線基準なので、見た目の縁取り幅の**2倍**を指定する。
- `lineWidth` には書体ごとの `font.strokeScale` を掛ける（4.3参照）。
- グラデは**キャンバス座標系**で定義される。`translate/rotate` を掛けた状態で
  `createLinearGradient` を作るとズレる。変形は `setTransform` で管理し、グラデ生成は変形後の座標で行う。
- `shadow` / `glow` は `ctx.shadowBlur` より、マスクをぼかして合成するほうが制御しやすい
  （`ctx.filter = 'blur(Npx)'` が使えるが Safari の挙動は要検証）。
- `letterSpacing` は Canvas2D の `ctx.letterSpacing` が使えるが Safari 対応が不確実（推測だが要検証）。
  フォールバックとして1文字ずつ手動配置する経路を用意する。`perChar: true` の場合は必ず手動配置。

#### 参考：サンプルGIF（金縁虹）の decorations

```ts
decorations: [
  { type: 'stroke', widthRatio: 0.10, color: { kind: 'metal', base: 'gold', angle: 90 } },
  { type: 'stroke', widthRatio: 0.04, color: { kind: 'solid', color: '#ffffff' } },
  { type: 'fill',   color: { kind: 'rainbow', saturation: 1, lightness: 0.5, cycles: 1, speed: 1, angle: 0 } },
]
```

### 4.3 フォント

Google Fonts の SIL OFL 日本語フォントのみを使用する。理由は2つ。
CDN配信のCSSをそのまま参照でき、かつ生成物の配布に制約が生じないため。
（OFLが制約するのはフォントソフトウェア自体の再配布であり、フォントで作った画像の利用ではない。
ただし法的判断が必要な場面ではライセンス原文を確認すること。）

#### 書体レジストリ

```ts
// core/fonts.ts
export interface FontDef {
  id: string;
  label: string;
  family: string;               // CSS font-family 名
  weight: number;
  /** Google Fonts CSS2 の URL */
  cssUrl: string;
  /** この書体で袋文字にするときの縁取り太さ倍率。細い書体ほど大きく */
  strokeScale: number;
  /** 文字が潰れやすい書体は少なめに */
  recommendedMaxChars: number;
  mood: string;                 // UIのグルーピング用
}

export const FONTS: FontDef[] = [
  { id: 'noto-black',   label: 'ゴシック（太）',   family: '"Noto Sans JP"',        weight: 900, strokeScale: 1.0, recommendedMaxChars: 20, mood: '標準' },
  { id: 'mplus-round',  label: '丸ゴシック',       family: '"M PLUS Rounded 1c"',   weight: 800, strokeScale: 1.0, recommendedMaxChars: 20, mood: 'ポップ' },
  { id: 'reggae',       label: 'インパクト',       family: '"Reggae One"',          weight: 400, strokeScale: 0.8, recommendedMaxChars: 12, mood: 'ネタ・強調' },
  { id: 'rocknroll',    label: '手書き風',         family: '"RocknRoll One"',       weight: 400, strokeScale: 1.1, recommendedMaxChars: 16, mood: 'カジュアル' },
  { id: 'shippori-b1',  label: '明朝（太）',       family: '"Shippori Mincho B1"',  weight: 800, strokeScale: 1.2, recommendedMaxChars: 16, mood: 'シリアス・ホラー' },
  { id: 'dotgothic',    label: 'ドット',           family: '"DotGothic16"',         weight: 400, strokeScale: 1.4, recommendedMaxChars: 12, mood: 'レトロゲーム' },
];
```

**まずこの6書体だけ実装する。** 追加は1エントリで済む構造にしておく。
`strokeScale` を持たせているのは、同じ `widthRatio` でも書体によって縁取りの見え方が
大きく変わるため。明朝やドットは字画が細く、標準の縁取り比率だと文字が潰れる。

TRPG用途を意識した選定になっている。明朝（`shippori-b1`）はクトゥルフのシリアスな演出、
ドット（`dotgothic`）はレトロゲーム風、`reggae` はサンプルGIFのようなネタ寄りのカットイン向け。

#### ロード戦略

```ts
async function ensureFont(font: FontDef, text: string): Promise<void> {
  await loadCss(font.cssUrl);                                  // <link> を1回だけ挿入
  await document.fonts.load(`${font.weight} 100px ${font.family}`, text);
}
```

- **描画前に必ず `await` する。** 省くと初回描画で豆腐（□）になる。
  フォント切替時とテキスト変更時の両方でチェックが要る。
- 日本語Webフォントは1書体あたり数MBあり、Google Fonts は unicode-range で
  100以上のサブセットに分割配信している。`document.fonts.load` に第2引数のテキストを渡すことで、
  実際に必要なサブセットだけが取得される。全書体を事前プリロードしてはいけない。
- **選択時ロード**が正解。UIでは書体選択直後に短いローディング表示を出す。
- 書き出し（フェーズ5以降）の直前にも `ensureFont` を呼ぶ。
  プレビューが表示できていても、全フレーム描画中にフォントが再評価される可能性がある（推測）。
- Google Fonts CSS2 API には `&text=` で指定文字のみのサブセットを返す機能があるが、
  テキストを変えるたびにURLが変わりキャッシュが効かない。**採用しない。**

#### プレビュー

書体選択UIは、**選択肢のラベルをその書体自身でレンダリングして見せる**。
日本語フォントは名前から見た目が想像できないため、テキストリストでは選べない。
ただし6書体すべてを一覧で表示すると全書体のロードが走るので、
ラベル数文字分だけを表示する軽量サムネイル（`ensureFont(font, font.label)`）にする。

---

### 4.4 文字装飾テンプレ（初期実装9種）

`decorations` 配列のプリセット。**カラーテーマ・演出テンプレとは独立した軸**として持つ（6章参照）。

| id | 名称 | 構成（背面→前面） | 想定 |
|---|---|---|---|
| `outline-gold` | 金縁袋文字 | stroke(gold, 太) → stroke(白) → fill | **既定**。サンプルGIF再現 |
| `outline-simple` | 袋文字（単色縁） | stroke(濃色) → fill | 汎用。可読性最優先 |
| `extrude-3d` | 3D押し出し | extrude(depth) → stroke → fill | インパクト系 |
| `neon` | ネオン発光 | glow(×3) → stroke(細) → fill(明色) | 暗背景専用。ホラー・サイバー |
| `hard-shadow` | ハードシャドウ | shadow(blur=0, offset大) → stroke → fill | ポップ・レトロ |
| `sticker` | ステッカー風 | shadow(soft) → stroke(白, 極太) → fill | 丸ゴシックと相性が良い |
| `glitch` | グリッチ | offsetCopy(赤, screen) → offsetCopy(シアン, screen) → fill | 狂気・SAN値減少演出 |
| `stripe` | ストライプ塗り | stroke(濃色) → pattern(stripe) | ポップ。虹ストライプが流れる |
| `knockout` | 背景くり抜き | 背景塗り → knockout | 帯型カットイン向け |

**まずこの9種を実装する。** `outline-gold` と `outline-simple` はフェーズ1〜3で作る
（`stroke` と `fill` の2デコレータだけで成立するため）。残りはフェーズ8。

各テンプレは相性制約を持つ。`neon` を白背景に置くと発光が見えず、
`knockout` は透過背景では成立しない。`compatible: { background: 'dark' | 'light' | 'any' }`
のようなヒントを持たせ、UIで警告する（禁止まではしない）。

---

## 5. カラーシステム

`ColorSpec` → `ColorRamp` のファクトリを `color.ts` に置く。

### rainbow

```
hue(u, t) = ((u * cycles + t * speed) * 360) mod 360
```

- `cycles=1, speed=1` で「1周期で虹が1周流れる」＝完全ループ。`speed` は整数のみ許可。
- `toGradient` は 13〜25 stop を打った `CanvasGradient` を返す。stop 数が少ないとバンディングする。
- `angle` で方向（横=0, 縦=90, 斜め=45）。

### metal（金縁）

サンプルGIFの縁は金メタリック。縦方向固定グラデで再現：

```
gold:   0.00 #6b4410 / 0.35 #ffe9a0 / 0.50 #c98a1e / 0.62 #fffdf0 / 1.00 #7a5210
silver: 0.00 #6a6a6a / 0.35 #f2f2f2 / 0.50 #9a9a9a / 0.62 #ffffff / 1.00 #707070
```

`angle` のみ可変。時間変化させない（金属感は静止しているほうが自然）。

### プリセットテーマ

| id | fill | strokes（外→内） | 背景 |
|---|---|---|---|
| `rainbow-gold` （サンプル再現） | rainbow(横, cycles=1) | metal:gold, solid:#fff | white |
| `rainbow-black` | rainbow(横) | solid:#000, solid:#fff | white |
| `gold-lux` | metal:gold | solid:#3b2400 | black |
| `pastel-pop` | linear(#ffd1dc→#c1e7ff) | solid:#fff | rainbow(低彩度, 低速) |
| `fire` | linear(#ffe259→#ff512f) | solid:#4a1500 | black |
| `ice` | linear(#e0f7ff→#2196f3) | solid:#0b3a5c, solid:#fff | white |
| `mono` | solid:#fff | solid:#000 | transparent |
| `custom` | ユーザー編集 | | |

**背景の既定は `transparent`。** カットインは盤面の上に重なるため、不透明背景だと盤面を隠してしまう。
APNGなら8bitアルファでアンチエイリアス縁がそのまま出る。

透過前提の配色上の注意：盤面の背景画像は明るいことも暗いこともある。
どちらでも文字が読めるよう、**最外周の縁は濃色（黒〜濃紺）かドロップシャドウにする**。
白1色の縁取りだけのテーマは明るい盤面で消えるため、プリセットには含めない。

---

## 6. テンプレの3軸と演出レイヤー

### 6.0 テンプレは3軸独立にする

| 軸 | 何を差し替えるか | 定義場所 | 初期実装 |
|---|---|---|---|
| **文字装飾** | `text.decorations` | 4.4 | 9種 |
| **カラーテーマ** | `decorations` 内の `ColorSpec` | 5章 | 8種 |
| **演出** | `layers` + `motion` | 6.1〜6.3 | 7種 |

3軸を独立に組み合わせられることが、MEGAMOJIに対する差別化の中身になる。
9×8×7 の組合せが機械的に生まれるため、テンプレ数を人力で増やす必要がない。

ただし全組合せが成立するわけではないので、**「レシピ」= 3軸のセット**を別途10〜15個用意し、
ギャラリーの入口にする。ユーザーはレシピを選んでから各軸を触る。
レシピの例：`成功（金縁虹＋集中線）`、`失敗（明朝＋暗い赤＋振動）`、`狂気（グリッチ＋ノイズ）`。

**UIは3タブに分ける。** 1画面に全パラメータを並べると破綻する。

### 6.1 演出レイヤーの実装

各レイヤーは `Layer` インタフェースを実装し、`layers/index.ts` のレジストリに登録する。
新規追加が1ファイル追加＋1行登録で済むこと。文字装飾パス（4.2）と同じ構造を意図的に揃えてある。

### 6.2 `radiate` — 集中線（サンプルGIF相当・最優先）

```
params: { count=48, minLen=0.10, maxLen=0.30, width=0.012, gap=0.02,
          colorMode='rainbow'|'match'|'solid', pulse=true, groups=2 }
```

- 中心 `center` から放射。角度は `i/count * 2π + rng()*jitter`（rngはフレーム間固定）。
- 内半径 `r0 = textBoxの外接円半径 * (1+gap)`、外半径 `r0 + len_i * size`。
- 色：角度に応じた hue（`hue = angle/2π*360 + t*360`）で、線ごとに虹が回る。
- アニメ：`groups=2` なら偶数本と奇数本を `t` で交互に表示（サンプルGIFはこの挙動に見える）。
  `pulse=true` なら長さを `sin(2πt)` で伸縮。
- 線は `lineCap='butt'`、太さは根元太く先端細くしたい場合は台形ポリゴンで描く（`fill` のほうが綺麗）。

### 6.3 その他の演出レイヤー（優先度順）

| id | 内容 | 主要params |
|---|---|---|
| `sparkle` | 四方向キラキラ星が明滅 | count, size, twinkleSpeed |
| `bgFlash` | 背景色が虹で循環 | speed, saturation |
| `confetti` | 紙吹雪が落下（画面外→画面外でループ） | count, speed |
| `speedLines` | 横方向の流線 | count, angle |
| `ring` | 同心円が広がって消える | count, speed |
| `noise` | 粗いノイズ（レトロ感） | amount |

### 6.4 文字全体のモーション（`motion`）

| type | 式 |
|---|---|
| `pulse` | `scale = 1 + amount * sin(2πt)` |
| `bounce` | `dy = -amount * abs(sin(πt)) * size` |
| `shake` | `dx,dy = amount * size * (noise2(t) 系、周期1)` |
| `rotate` | `angle = 2πt * amount`（amount は整数） |
| `wave` (perChar) | 各文字 `dy = amount*size*sin(2π(t - i/n))` |

### テンプレ定義の形

テンプレ＝`RenderParams` の部分適用（テキスト以外を上書き）。JSONで持ち、ギャラリーに並べる。

```ts
export const TEMPLATES: Array<{ id: string; label: string; thumb: string; patch: Partial<RenderParams> }> = [...]
```

---

## 7. 書き出し

### 7.1 パイプライン

```ts
// メインスレッド
const { w, h } = params.canvas;
const frames: ImageData[] = [];
for (let i = 0; i < N; i++) {
  render(ctx, params, i / N);
  frames.push(ctx.getImageData(0, 0, w, h));
}
worker.postMessage({ frames, params }, frames.map(f => f.data.buffer));
```

### 7.2 Worker: APNG（既定・UPNG.js）

```ts
import UPNG from 'upng-js';

const delays = new Array(N).fill(Math.round(1000 / params.fps));
const buffers = frames.map(f => f.data.buffer);
// cnum=0 で無損失、256 で量子化。アルファは保たれる
const apng: ArrayBuffer = UPNG.encode(buffers, w, h, params.output.colors, delays);
const blob = new Blob([apng], { type: 'image/png' });
```

- **UPNG.encode はフレーム間差分と dispose/blend を内部で処理する**（推測。フェーズ5で実測）。
  静止領域が多いテンプレほどサイズが小さくなるはず。
- APNG のフレーム遅延は分数（delay_num / delay_den）で表現されるため、GIFのような
  10ms刻み丸めの制約がない。20〜30fps を自由に選べる。
- 再生回数は num_plays。**0（無限ループ）を必ず指定する**。UPNG.js が指定できない場合、
  出力バッファの acTL チャンクを直接書き換える後処理を入れる（32バイト固定位置の探索で足りる）。

### 7.3 Worker: GIF（Discordチャット添付用・gifenc）

**互換用ではなく必須機能。** Discordのチャット添付ではAPNGがアニメーションしないため、
この経路が使えないとローカル保存→送信のユースケースが成立しない。

```ts
import { GIFEncoder, quantize, applyPalette } from 'gifenc';
const gif = GIFEncoder();
const delay = Math.max(20, Math.round(1000 / params.fps / 10) * 10); // ms、10ms刻み
for (const frame of frames) {
  const src = params.output.matte ? matte(frame, params.output.matte) : frame;
  const palette = quantize(src.data, 256, { format: 'rgb565' });
  const index = applyPalette(src.data, palette, 'rgb565');
  gif.writeFrame(index, w, h, { palette, delay });
}
gif.finish();
```

GIFのdelayは1/100秒単位で、ブラウザは歴史的に `delay <= 10ms` を100msへ丸める実装がある。
**20ms以上を必ず保証する**。

#### マット合成（GIFの1bit透過問題への対処）

GIFで透過を有効にすると、袋文字のアンチエイリアス縁が白いギザギザのハロになる。
Discordのチャットは暗いテーマ上に表示されるため、これが特に目立つ。

```ts
/** 半透明ピクセルを指定色に対して合成し、完全不透明の画像にする */
function matte(img: ImageData, bg: string): ImageData
```

- `output.matte` に色を指定すると、その色で合成して**不透明GIF**を出力する。
- Discordチャット添付の既定値は `#313338`（Discordダークテーマの背景色。要検証）。
  ライトテーマのユーザーには境界が見えるが、透過ギザギザよりは自然。
- ココフォリア向けにGIFを出す場合は、盤面背景が不定なのでマット色をユーザーに選ばせる。
- **APNG出力時はマット合成を行わない**（8bitアルファをそのまま活かす）。

### 7.4 出力ターゲット抽象（将来のプラットフォーム追加に備える）

**ココフォリア専用にハードコードしない。** 配布先ごとの制約を記述子として持ち、
UIとバリデータをそこから駆動する。追加は1エントリで済むようにする。

```ts
// core/targets.ts
export interface ExportTarget {
  id: string;
  label: string;
  /** null = 自由。数値指定は「厳密一致が必要」を意味する */
  fixedSize: { w: number; h: number } | null;
  defaultSize: { w: number; h: number };
  /** 容量に余裕があるターゲットでは高品質側に倒す */
  defaultFrames: number;
  defaultFps: number;
  maxBytes: number;
  /** 使用可能な形式。先頭が既定 */
  formats: OutputFormat[];
  /** 透過が使えるか（GIFしか使えないターゲットは実質1bit） */
  alpha: 'full' | '1bit' | 'none';
  /** 実際にユーザーの画面で表示される概算px。可読性チェックに使う */
  displaySize: number;
  /** 推奨される最大文字数。超えたら警告 */
  maxChars: number;
  notes: string[];
}

export const TARGETS: ExportTarget[] = [
  {
    id: 'ccfolia-cutin',
    label: 'ココフォリア カットイン',
    fixedSize: null,
    defaultSize: { w: 480, h: 480 },
    defaultFrames: 15,
    defaultFps: 20,
    maxBytes: 1_000_000,
    formats: ['apng', 'gif', 'png'],
    alpha: 'full',
    displaySize: 480,
    maxChars: 20,
    notes: ['音源を設定しない場合、操作するまで消えない（無限ループ）'],
  },
  {
    id: 'discord-sticker',
    label: 'Discord ステッカー',
    fixedSize: { w: 320, h: 320 },      // 厳密一致が要求される
    defaultSize: { w: 320, h: 320 },
    defaultFrames: 12,
    defaultFps: 20,
    maxBytes: 512_000,
    formats: ['apng', 'png'],           // GIFは受け付けられない
    alpha: 'full',
    displaySize: 160,
    maxChars: 8,
    notes: ['320×320ちょうどでないと弾かれる', 'GIF不可。アニメはAPNG'],
  },
  {
    id: 'discord-attachment',
    label: 'Discord チャット添付（ローカル保存して送信）',
    fixedSize: null,
    defaultSize: { w: 720, h: 720 },    // 容量に余裕があるので高解像度
    defaultFrames: 30,
    defaultFps: 30,
    maxBytes: 8_000_000,                // 無料10MBに対し安全側
    formats: ['gif'],                   // ★ APNGはDiscordが静止画化するため使用不可
    alpha: '1bit',
    displaySize: 400,                   // 埋め込み表示の概算
    maxChars: 20,
    notes: [
      'DiscordはチャットのAPNGをアニメーションさせない（1フレーム目のみ表示）',
      '容量に余裕があるので高解像度・高フレームレートにできる',
      '暗いテーマ上に表示されるためマット合成を推奨',
    ],
  },
];
```

**Discord絵文字（128×128・GIFのみ・表示32px）は対象外。** 表示32pxでは複数文字の可読性が
成立せず、別デザインモードが必要になるため、費用対効果が合わない。将来やるなら
`TARGETS` に1エントリ追加＋文字数制約モードの実装、という切り分けになる。

**バリデータ** `validate(params, target): Issue[]` を用意し、書き出しボタンの近くに常時表示する。
検査項目：形式の可否、寸法の厳密一致、推定バイト数、文字数、透過の整合。

### 7.5 可読性の制約

配布先によって実表示サイズが変わる。ココフォリアのカットインは約480px、
Discordステッカーは約160pxで、後者は文字が半分以下の大きさになる。

したがって、**ターゲットに応じた自動調整を `render` の前段に挟む**。

```ts
function adaptForTarget(params: RenderParams, target: ExportTarget): RenderParams
```

| 調整項目 | 内容 |
|---|---|
| 縁取り太さ | `displaySize` が小さいほど `widthRatio` を増やす |
| エフェクト密度 | `radiate.count` を `displaySize/480` で線形に減らす。下限8本 |
| 文字数警告 | `text` の総文字数 > `target.maxChars` でUIに黄色警告（禁止ではなく注意） |
| 動きの振幅 | 小サイズでは `motion.amount` を抑える（振れ幅が相対的に大きく見えるため） |
| **解像度・フレーム数** | ターゲット切替時、ユーザーが明示的に触っていない項目は `defaultSize` / `defaultFrames` / `defaultFps` に追従させる |

**品質の引き上げも `adaptForTarget` の仕事にする。** Discordチャット添付は容量が実質無制限
（無料枠10MB）なので、ココフォリア用の 480px / 15f / 20fps をそのまま流用するのは機会損失。
同じデザインを 720px / 30f / 30fps で出せる。
逆にステッカーは512KB制約が厳しいので落とす方向に働く。

実装上の注意：**ユーザーが手動で変更した項目は上書きしない。**
`RenderParams` とは別に「どのフィールドがユーザー編集済みか」の集合を持ち、
未編集のフィールドだけターゲット既定値に追従させる。
これを怠ると、解像度を手で設定した直後にターゲットを切り替えて値が消える、という不快な挙動になる。

`adaptForTarget` は純関数として `core/targets.ts` に置き、プレビューにも同じものを通す。
**プレビューには実際の表示サイズでの見え方を併記する**（原寸と `displaySize` 縮小の並置）。

ステッカーは320×320で作って160×160で表示されるため、集中線の本数を480px想定のまま
持ち込むと潰れる。ここは実機で目視確認する。

### 7.6 サイズ管理

サンプルGIFは 500×500 / 18f で **718KB**。ターゲットごとの予算は `maxBytes` に従う。

書き出し後に実バイト数と予算比を表示し、**超過時は自動軽量化ボタン**を出す。
軽減の適用順（効果が大きく劣化が小さい順）：

1. `output.colors` を 256 → 128 → 64 に下げる
2. フレーム数を間引く（15 → 12 → 10）
3. 集中線の本数など、高周波なレイヤーのパラメータを下げる（差分圧縮が効きやすくなる）
4. キャンバスを縮小する（`fixedSize` が指定されたターゲットでは不可）

### 7.7 ココフォリア向けサイズプリセット

`ccfolia-cutin` ターゲット内の寸法選択肢。

| プリセット | w×h | frames | fps | 想定 |
|---|---|---|---|---|
| カットイン（正方形） | 480×480 | 15 | 20 | **既定** |
| カットイン（大） | 600×600 | 18 | 20 | BOOTH配布素材の定番寸法 |
| 横帯 | 1200×300 | 15 | 20 | 画面中央に細く出す演出 |
| ワイド | 800×450 | 15 | 20 | 16:9 |
| 軽量 | 320×320 | 10 | 15 | 容量が厳しいとき |

### 7.8 一括書き出し（差別化機能）

ココフォリアのカットインはチャット末尾の文字列で発火するため、
**ダイス結果に対応した数種類をセットで用意する**のが実運用。
テキストだけを差し替えた複数枚を1回の操作で生成し、ZIPでまとめてDLする機能を作る。

既定セット（クトゥルフ神話TRPG想定）:

```
決定的成功 / 成功 / 失敗 / 致命的失敗 / 正気度喪失 / シークレットダイス
```

その他のプリセット群: `成功|失敗`、`勝利|敗北`、`KPは困っています`（サンプル再現）。
UIには「ココフォリアのカットイン名にこの文字列を設定してください」という案内を添える。

---

## 8. UI設計

```
┌──────────────┬───────────────────┐
│   プレビュー    │  ① テキスト        │
│  （rAFで実時間  │  ② テンプレギャラリ  │
│    ループ再生）  │  ③ カラーテーマ     │
│               │  ④ 演出パラメータ    │
│  [GIFを書き出す]│  ⑤ 出力設定        │
└──────────────┴───────────────────┘
```

- **プレビューはGIFを作らない。** `requestAnimationFrame` で `render(ctx, params, (now/period)%1)` を回す。
  パラメータ変更が即座に反映される。GIF生成はボタン押下時のみ（重いため）。
- 書き出し中はプログレスバー（Workerから進捗postMessage）。
- **URL共有**：`serialize.ts` で `RenderParams` を JSON→ `TextEncoder` → base64url にして `location.hash` に入れる。
  1KB程度に収まるはず。テンプレ配布とバグ報告が劇的に楽になるので、これは初期から入れる。
- モバイル対応：プレビュー上・コントロール下の1カラムに落とす。

---

## 9. 実装フェーズ

各フェーズの終わりで動作確認できる順序にしてある。

| # | 内容 | 完了条件 |
|---|---|---|
| 1 | 静止レンダラ | 固定文字列・固定色で袋文字PNGが1枚出る |
| 2 | 自動フォントサイズ＋改行＋**書体レジストリ（6書体）** | 1文字〜20文字×3行、全書体で枠内に収まる。書体切替でフォントロードを待てている |
| 3 | ColorRamp（rainbow/metal）＋デコレータ `stroke`/`fill` | サンプルGIFの静止1フレームが `decorations` 経由で再現できる |
| 4 | 時間軸 `t` 導入＋プレビュー | rAFで虹が流れる。ループ継ぎ目なし |
| 5 | **APNG書き出し（Worker）＋ココフォリア実機確認** | 生成物をココフォリアにアップし、カットインとして透過・ループ再生されることを目視確認 |
| 6 | **GIF書き出し＋マット合成** | Discordのチャットに添付して動くことを確認 |
| 7 | `radiate` レイヤー | サンプルGIFがほぼ再現できる |
| 8 | 文字装飾デコレータ拡充（残り7種）／演出レイヤー／カラーテーマ | 3軸それぞれが独立に切り替えられる |
| 8.5 | レシピ（3軸セット）とギャラリーUI | 10〜15レシピから選んで即書き出せる |
| 9 | 一括書き出し（ZIP）・文言プリセット | ダイス結果6種を1操作で生成できる |
| 10 | URL共有・出力プリセット・サイズ警告 | 共有URLを開くと同じ絵が出る |
| 11 | Discordステッカー対応（320固定・512KB） | 実際にステッカーとして登録できる |
| 12 | UI仕上げ・モバイル対応 | |

**フェーズ5と6を連続で置くのが要点。** APNGとGIFは配布先ごとに必須形式が反転するため、
片方だけ作った状態は「半分の用途で使えないツール」であって、リリース可能な状態ではない。
両方通してからテンプレ拡充に進む。

**`ExportTarget` の型定義と `TARGETS` 配列はフェーズ1から置いておく。**
後から「サイズ」を単一値からターゲット依存に変えると、
レイアウト・プレビュー・書き出しの全経路に手が入り、確実に事故る。
中身がココフォリア1件だけでも、参照経路を最初から通しておくこと。

**フェーズ5に実機確認を含めているのが要点。** UPNG.js が出す acTL の num_plays、
アルファ量子化の品質、そしてココフォリア側の容量制限の実値は、ドキュメントからは確定できない。
ここで想定と違えば以降の設計が変わるため、テンプレを増やす前に必ず1枚を実機に通す。

---

## 10. テスト方針

- `core/text/layout.ts`：文字数・行数を変えて枠内に収まることをアサート（Vitest）。**全6書体で回す。**
- `core/fonts.ts`：`ensureFont` を呼ばずに描画した場合に警告を出す開発時アサートを入れる（豆腐対策）。
- `core/color.ts`：`sample(u, 0)` と `sample(u, 1)` が一致すること（ループ検証）。
- レイヤー／文字装飾デコレータ：`draw(ctx, 0)` と `draw(ctx, 1)` のImageDataが一致すること（ループ検証、許容誤差あり）。
- デコレータ：全9テンプレ × 全6書体の組合せを1枚ずつ書き出し、目視確認用のコンタクトシートを生成する
  スクリプトを用意する（54枚。手動確認では抜けが出る）。
- E2Eは不要。目視確認で足りる。

---

## 11. 未確定事項（フェーズ5までに実測して確定させる）

| # | 項目 | 確認方法 | 影響 |
|---|---|---|---|
| 1 | ココフォリアのアップロード容量上限の実値（1MB説） | 段階的にサイズを変えたAPNGを実際にアップロードする | 出力プリセット全体 |
| 2 | カットイン表示の最大寸法（480px説は2020年の情報） | 大小2枚を実機で再生し表示サイズを比較 | 既定解像度 |
| 3 | UPNG.js が num_plays=0（無限ループ）を出力するか | 生成物のacTLチャンクをバイナリで確認 | ループ後処理の要否 |
| 4 | UPNG.js のアルファ量子化品質（cnum=256で袋文字の縁が保たれるか） | 目視＋ファイルサイズ計測 | 減色戦略 |
| 5 | Safari の `ctx.letterSpacing` 対応 | 実機確認 | 未対応なら1文字ずつ手動配置を既定にする |
| 6 | ココフォリアがAPNGを静止PNGとして扱う環境がないか | 別ブラウザ・スマホで再生確認 | GIFフォールバックの優先度 |
| 7 | Discordステッカーの仕様（320×320厳密・512KB・APNG可/GIF不可） | 二次情報の一致のみ。実際にアップロードして確認 | ターゲット記述子の値 |
| 8 | DiscordチャットでのAPNG静止画化が現在も起きるか | 生成したAPNGを実際にチャットへ添付して確認 | 添付用の必須形式（GIF確定かどうか） |
| 9 | Discordダークテーマの背景色（`#313338` と理解） | 実機のスクリーンショットからスポイトで取得 | マット合成の既定値 |

いずれも公式ドキュメントに記載がないか、情報源が古い。**推測で実装を進めず、必ず実機で確定させること。**